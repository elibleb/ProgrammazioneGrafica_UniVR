#version 330 core

out vec4 FragColor; //variabile in uscita, colore finale sullo schermo

void main()
{
    FragColor = vec4(1.0); //vettore con tutti i componenti (R, G, B, A) impostati a 1.0, colore bianco massimo opaco
}